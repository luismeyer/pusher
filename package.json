{
  "name": "pusher",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "watch": "lerna run watch --parallel",
    "ts": "lerna run ts",
    "dev": "lerna run dev --parallel",
    "build": "lerna run build",
    "synth": "lerna run synth",
    "deploy:aws": "lerna run deploy --scope=aws",
    "deploy:vercel": "npx vercel deploy",
    "setup": "node scripts/setup.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.294.0",
    "@types/node": "^18.14.0",
    "decompress": "^4.2.1",
    "decompress-tarxz": "^3.0.0",
    "decompress-unzip": "^4.0.1",
    "lerna": "^6.5.1",
    "typescript": "^4.9.5",
    "vercel": "^28.17.0"
  }
}

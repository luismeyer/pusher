{
	"name": "pusher",
	"version": "1.0.0",
	"description": "",
	"keywords": [],
	"license": "ISC",
	"author": "",
	"scripts": {
		"build": "turbo run build",
		"build:watch": "turbo run build:watch",
		"start": "turbo run dev build:watch compile:watch --concurrency 25",
		"dev:scheduler": "pnpm run --filter @pusher/aws dev:scheduler",
		"setup-bucket": "node scripts/setup.mjs",
		"compile": "turbo run compile",
		"compile:watch": "turbo run compile:watch"
	},
	"devDependencies": {
		"@aws-sdk/client-s3": "^3.294.0",
		"@biomejs/biome": "2.2.4",
		"@types/node": "^22.18.7",
		"decompress": "^4.2.1",
		"node-fetch": "^3.3.1",
		"turbo": "^1.10.3",
		"typescript": "^5.3.3"
	}
}

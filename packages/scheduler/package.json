{
  "name": "@pusher/scheduler",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js --platform=node",
    "dev": "npm run build -- --watch",
    "ts": "tsc --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "duenamodb": "^1.3.0",
    "@aws-sdk/client-lambda": "^3.289.0",
    "@pusher/shared": "1.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.17.11"
  }
}
